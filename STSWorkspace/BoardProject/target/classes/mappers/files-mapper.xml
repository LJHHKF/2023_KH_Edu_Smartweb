<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Files">
	<insert id="insert">
		insert into FILES(SEQ, ORINAME, SYSNAME, PARENT_SEQ)
		 values(FILES_SEQ.nextval, #{oriName}, #{sysName}, ${parent_seq})
	</insert>
	
	<select id="selectByParentSeq" resultType="kh.spring.dto.FilesDTO">
		select * from FILES where PARENT_SEQ=${parent_seq} order by SEQ
	</select>
</mapper>